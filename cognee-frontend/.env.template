# Backend and MCP URLs for local development
# These are used by Next.js rewrites to proxy API requests
# In Kubernetes, these are set as environment variables in the deployment
BACKEND_URL=http://localhost:8000
MCP_URL=http://localhost:8001

# Cloud API URL (if using cloud environment)
# NEXT_PUBLIC_CLOUD_API_URL=https://api.cognee.cloud
