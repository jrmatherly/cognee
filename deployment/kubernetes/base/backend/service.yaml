---
apiVersion: v1
kind: Service
metadata:
  name: cognee-backend
  labels:
    app.kubernetes.io/name: cognee
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: cognee
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: cognee
    app.kubernetes.io/component: backend
